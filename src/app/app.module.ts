import { NgModule } from '@angular/core';
import { AppComponent } from './app.component';
import { BrowserModule } from '@angular/platform-browser';

@NgModule({
  //declare and register all the compoenent that need to work together
  declarations: [], //used to add non standalone component
  bootstrap: [], //root component
  imports: [], //used to add a standalone component inside module
})
export class AppModule {}
